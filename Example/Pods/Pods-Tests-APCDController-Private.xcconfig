#include "Pods-Tests-APCDController.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/APCDController" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/APCDController"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}